

const initialState = {
    glibres: {},
    tlibres: {}
    
}

const reducerApp = (state = initialState, action) => {
    switch (action.type) {
      
        default:
            return state;
    }
}


export default reducerApp;
